// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

package frc.robot.commands;

import edu.wpi.first.wpilibj2.command.CommandBase;
import frc.robot.subsystems.Arm;
import frc.robot.subsystems.Bling;
import frc.robot.subsystems.Claw;
import frc.robot.subsystems.OI;

public class ArmOperatorMode extends CommandBase {
    /*Creates a new ArmOperatorMode that sets buttons for cube placement or cone placement 
     * Note to self: veiw button left, menu button right
     * @param arm is the 
     * 
    */
    private Arm arm;
    private 


}

public ArmOperatorMode(arm, ){
    this.arm = arm;
    this.

}

@Override
  public void initialize() {
    arm.setTrapezoidTargetAngle(arm.new JointPositions(shoulderAng, elbowAng));
  }

  // Called every time the scheduler runs while the command is scheduled.
  @Override
  public void execute() {
    
  }

  // Called once the command ends or is interrupted.
  @Override
  public void end(boolean interrupted) {}

  // Returns true when the command should end.
  @Override
  public boolean isFinished() {
    return false;
  }


